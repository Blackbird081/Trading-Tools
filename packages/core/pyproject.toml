[project]
name = "core"
version = "0.1.0"
description = "Domain layer — entities, ports, use cases. Zero external dependencies."
requires-python = ">=3.12"
dependencies = []  # ★ ZERO external dependencies — pure Python only

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
